namespace MapEditor.Commands
{
    public enum CommandType
    {
        None = 0,
        Move,
        Stop,
        Undo,
        Redo,
        PlaceTile,
        AddUnit,
        MoveCamera
    }
}
